<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a4ic1.projektkoncowopolroczny">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Albums.ChooseAlbumsActivity" />
        <activity android:name=".Camera.CameraActivity" />
        <activity android:name=".Albums.GalleryActivity" />
        <activity android:name=".Albums.PhotoActivity" />
        <activity
            android:name=".Camera.MakePhotoActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".Collage.ChooseCollageActivity" />
        <activity android:name=".Collage.CollageActivity" />
        <activity android:name=".Collage.CollageCameraActivity" />
        <activity android:name=".Albums.FontsActivity" />
        <activity android:name=".NetworkActivity"></activity>
    </application>

</manifest>